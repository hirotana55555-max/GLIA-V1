# SYSTEM RELATIONS

Mission Flow is unidirectional:
Human → Swarm → LLM → SIE → Executor → Agent

AuditLogger:
- Observes
- Records
- Never intervenes

LLM:
- Proposes
- Never decides authority

Browser:
- Sees
- Acts
- Does not think

API and LLM outputs are probabilistic and hostile by default.
